import appendSpaces from './append-spaces';
import createList from './create-list';
import formatProduct from './format-product';
import limitSize from './limit-size';
import separeWords from './separe-words';

const strings = { appendSpaces, createList, formatProduct, limitSize, separeWords };
export default strings;
